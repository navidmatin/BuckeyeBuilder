<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".BuildPlanActivity" >

    <Button
        android:id="@+id/buildbutton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
      
      	android:layout_weight="2"
        android:text="Build" />
    
    <Button
        android:id="@+id/managebutton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/managebutton"
        android:layout_alignBottom="@+id/managebutton"
        android:layout_toLeftOf="@+id/managebutton"

      	android:layout_weight="2"
        android:text="Manage" />

    <Button
        android:id="@+id/planbutton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/planbutton"
        android:layout_alignBottom="@+id/planbutton"
        android:layout_toRightOf="@+id/planbutton"
      	android:layout_weight="3"
        android:text="Build_Plan" />

    

</LinearLayout>